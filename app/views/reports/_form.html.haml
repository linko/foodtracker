= simple_form_for @report, :html => { :class => 'form-horizontal' } do |f|
  = f.input :period_start, as: :hidden
  = f.input :period_end, as: :hidden
  = f.input :email
  = f.input :subject
  = f.input :comments
  .form-actions
    = f.button :submit, :class => 'btn-primary'